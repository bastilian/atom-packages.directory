development:
  clients:
    default:
      uri: <%= ENV['MONGODB_URI'] %>

production:
  clients:
    default:
      uri: <%= ENV['MONGODB_URI'] %>

test:
  clients:
    default:
      uri: <%= ENV['MONGODB_URI'] %>
